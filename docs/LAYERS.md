# NOESIS Layers Bridge Note

The canonical 32 REALMS contract now lives at the repository root in `LAYERS.md`.

Use root `LAYERS.md` for:

- the 32 REALMS inventory
- taxonomy categories: `WORLD`, `OVERLAY`, `SENSE`, `GATE`, `ADMIN`
- governance rules and saturation consequences
- the pseudo-ALL reset direction

This `docs/` page is a bridge note only.
It preserves the existence of a `docs/` landing point while deferring current repository truth to root `LAYERS.md`.
